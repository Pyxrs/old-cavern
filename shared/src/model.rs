// TODO Client only

use std::fs::ReadDir;

use crate::types::Id;

pub struct BlockModel {
    //
}

pub struct ItemModel {
    //
}

pub fn load_item_model(addon_path: &ReadDir, id: &Id) -> ItemModel {
    ItemModel {
        //
    }
}